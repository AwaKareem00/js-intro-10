let str = '    Tech        Global       ';

console.log(str.trim());
console.log(str.trimStart());
console.log(str.trimEnd());

let greeting = 'Hello';

console.log(greeting.trim());

/*
trim()
TASK: removes the whitespaces at the beginning and the end of the string
RETURN: a new modified string
ARGUMENTS: no args
DOES IT CHANGE OG VALUE: no does not modify og value
STATIC OR NON: non static // starts with a variable like str instead of Number, Math...
*/

